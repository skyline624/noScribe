# Install with `pip install -r requirements_win_zluda.txt`
# ZLUDA support for AMD GPUs (RX 6800 series) on Windows
# ZLUDA translates CUDA calls to ROCm at runtime
# Note: Requires ZLUDA to be installed and ZLUDA_PATH environment variable set

# PyTorch with CUDA support (ZLUDA will translate to ROCm)
--index-url https://download.pytorch.org/whl/cu121
torch==2.5.1
torchaudio==2.5.1

# Standard dependencies (from PyPI)
--extra-index-url https://pypi.org/simple
AdvancedHTMLParser
appdirs
cpufeature
customtkinter
CTkToolTip
faster-whisper
Pillow
pyannote.audio>=4.0
pyinstaller==6.14.1
# The bootloader in the most recent version of pyinstaller is often falsely detected as malware
# by anti virus software, so, it's safer to use a version that has been released a few months ago.
python-i18n
PyYAML
